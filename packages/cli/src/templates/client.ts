export type ClientTemplateOptions = {
  apiKey: string;
};

export const clientTemplate = (opts: ClientTemplateOptions): string =>
  `// This file is auto-generated by the EdgePod CLI.
//
// The API key below is a public/publishable key — it is sent with every
// request to your EdgePod backend and is safe to include in client bundles.
//
// For production, replace the \`url\` with your deployed worker URL.
// Run \`edgepod info\` after deploying to get it.
import { createEdgePodClient } from "@edgepod/client";
import type { EdgePodRouter } from "./router";

export const edgepod = createEdgePodClient<EdgePodRouter>({
  url: "http://localhost:8989",
  apiKey: "${opts.apiKey}",
});
`;
