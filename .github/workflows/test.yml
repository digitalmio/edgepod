name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-client:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        react-version: [18, 19]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Override React version (matrix)
        if: matrix.react-version == 19
        run: |
          # Force React 19 across the monorepo for this CI run.
          # react-dom must match react or @testing-library/react crashes.
          pnpm add --save-dev react@^19 react-dom@^19 @types/react@^19

      - name: Run tests
        run: pnpm exec vitest run

      - name: Type check
        run: pnpm --filter @edgepod/client exec tsc --noEmit
